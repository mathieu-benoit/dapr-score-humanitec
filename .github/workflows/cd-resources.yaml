name: cd-resources
permissions:
  contents: read
  id-token: write
on:
  push:
    branches:
      - main
env:
  HUMCTL_VERSION: '0.11.0'
jobs:
  deploy-humanitec:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: install humctl
        uses: humanitec/setup-cli-action@v1
        with:
          version: ${{ env.HUMCTL_VERSION }}
      - name: apply humanitec resources
        run: |
          # FIXME: automate this
          humctl apply \
              -f resources/dapr-workload.yaml \
              --org ${{ secrets.HUMANITEC_ORG }}
          humctl apply -f \
              -f resources/in-cluster-redis.yaml \
              --org ${{ secrets.HUMANITEC_ORG }}
          humctl apply -f \
              -f resources/redis-dapr-state.yaml \
              --org ${{ secrets.HUMANITEC_ORG }}